# Repository naming

Approved repository name: `evidra-rag`.
Historical source folder: `MODULAR-RAG-MCP-SERVER`.
The folder/repository rename does not change package imports, CLI commands, dataset hashes,
model weights or the scope of prior experimental evidence.

The development folder is now `E:/AI-Agent-Lab/projects/evidra-rag`; the old folder name is a
local compatibility junction, not a second copy of the project.
The public upload is prepared from the audited source release in a separate Git worktree,
so historical development Git objects and private runtime data are not published.
