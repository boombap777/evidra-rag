# Third-party notices

MIT applies to this project's own source only. Dependency distributions retain their
own license/copyright files; the selected versions are recorded in uv.lock. Chroma,
Streamlit, MCP, LangChain, MarkItDown, Ragas, PyMuPDF and their dependencies are installed
separately, not relicensed by this repository. In particular, review PyMuPDF's own
distribution terms before using it in a redistributed or hosted derivative product.

The three knowledge-workspace examples in fixtures/publication_demo.json are original
synthetic demo material. Legacy sample PDFs generated by the checked-in fixture scripts
are test inputs, not business documents. The unverified blogger-introduction PDF/generator
and Vision photograph are deliberately excluded from the source publication policy.

The full Stack Overflow benchmark uses mteb/stackoverflowdupquestions-reranking. Its
content is not MIT-licensed merely because this code is; obtain it from its original
dataset distribution and preserve the applicable Stack Overflow attribution/share-alike
terms. Corpus text, cached vectors and model weights are not bundled here.

Qwen3-Embedding, cross-encoder/ms-marco-MiniLM-L-6-v2 and llama.cpp are separate artifacts.
Obtain them from their official distributions and retain their model/software licenses.
The offline demonstration requires none of those model downloads.
